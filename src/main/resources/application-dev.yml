spring:
  datasource:
    url: jdbc:h2:mem:exequt;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    driver-class-name: org.h2.Driver
    username: sa
    password:

  h2:
    console:
      enabled: true
      path: /h2-console

logging:
  level:
    root: INFO
    com.exequt: DEBUG